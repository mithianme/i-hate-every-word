# paste your four values from developer.x.com -> your app -> Keys and tokens
# (no quotes needed, just paste straight after the = sign)
X_API_KEY={INSERT}
X_API_SECRET={INSERT}
X_ACCESS_TOKEN={INSERT}
X_ACCESS_TOKEN_SECRET={INSERT}
